#include <stdio.h>

int main() {
    int q;
    long long int n;
    scanf("%d", &q);
    for (int query = 0; query < q; query++) {
        scanf("%lld", &n);
        printf("%d\n", 0);
    }  
    return 0;
}
